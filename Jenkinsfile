pipeline {
    agent any 
    stages {
       stage("Build") {
	  steps {
	    echo "Hi this a build stage."
	  }
    }

       stage("Test") {
	  steps {

	  }
       }

       stage("deploy") {
	  steps {
		
	  }
    }
   }
}
